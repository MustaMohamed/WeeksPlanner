{
  "type": "sqlite",
  "database": "plan.db",
  "entities": [
    "dist/**/*.entity{.ts,.js}"
  ],
  "synchronize": true,
  "migrationsTableName": "migrations",
  "migrations": ["migration/*{.js,.ts}"],
  "cli": {
    "migrationsDir": "migration"
  }
}